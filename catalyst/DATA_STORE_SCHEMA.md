# Catalyst Data Store — Police FIR schema

Implementation guide for mapping the Karnataka Police FIR ER diagram
(`Police_FIR_ER_Diagram.pdf`, root of the workspace — the organizer-supplied
source of truth) onto **Catalyst Data Store**.

## ⚠️ Two Catalyst projects — build/query against the right one
- **`RakshaDrishti`** (`56806000000019001`) — the **original submitted**
  project. Frozen as of the KSP Datathon26 submission: `District`(8),
  `Unit`(8), `CrimeSubHead`(4), `CaseMaster`(406) only. **Do not modify** —
  it backs the public submission repo's live demo link. Leave it exactly as
  judges saw it.
- **`Raksha-Dhrishti-v2`** (`56806000000070001`) — the **active** project for
  all continued work (private repo). This is the one to build/import/query
  against. Switch to it locally with `catalyst.cmd project:use Raksha-Dhrishti-v2`.

## Status (as of 2026-08-21, in `Raksha-Dhrishti-v2`)

All **21 backbone tables** below (the full "Table → column reference"
section) are **built and schema-verified** — every table exists with the
right columns, confirmed by querying each one directly
(`catalyst ds:export --table <Name>`). **Zero rows imported yet** — schema
only, data import is the next step (seed CSVs are ready in `catalyst/seed/`,
generated by `gen_seed.py`).

Two tables were built *more complete* than the seed CSVs / this doc's
original column list — matching the raw ER diagram more closely, which is
correct, not a bug:
- `Employee` also has `BloodGroupID`, `PhysicallyChallenged` (present in the
  ER diagram, missing from `Employee.csv`'s columns).
- `Unit` also has `ParentUnit`, `NationalityID` (same story vs `Unit.csv`).

These extra columns just won't get populated when importing the existing
seed CSVs (import matches by column name) — harmless, but worth knowing
before assuming a `NULL` there means something's wrong.

**Not yet built** — 6 more tables the ER diagram defines beyond the 21-table
backbone (arrest tracking + complainant demographic lookups). See
[Extended tables](#extended-tables-not-yet-built) below for full column specs
before building these — don't re-derive them from the raw PDF each time.

## How the mapping works

Catalyst Data Store is relational, so the ER diagram maps ~1:1.

- **Primary keys** — every Data Store table has an automatic `ROWID` PK. Keep the
  schema's own IDs (`CaseMasterID`, `DistrictID`, …) as **Number** columns so the
  business keys and foreign keys still line up. Seed them explicitly.
- **Foreign keys → Lookup columns.** A `Lookup` column references another table
  and *is* the FK. It enforces the relationship and enables JOINs in **ZCQL**
  (Catalyst's SQL). Where you prefer to seed by CSV first and wire relationships
  later, use a plain **Number** column and convert it to a Lookup afterwards.
- **Queries** use ZCQL from a Function, e.g.
  `SELECT CaseMaster.CrimeNo, District.DistrictName FROM CaseMaster
   LEFT JOIN Unit ON CaseMaster.PoliceStationID = Unit.UnitID
   LEFT JOIN District ON Unit.DistrictID = District.DistrictID`.

### Type mapping
| ER diagram type | Catalyst column type |
|---|---|
| `INT` (PK/FK/plain) | Number (Lookup for FKs) |
| `VARCHAR`, `CHAR`, `NVARCHAR(MAX)` | Text |
| `DATE` | Date |
| `DATETIME` | DateTime |
| `BIT` | Boolean |
| `DECIMAL` (lat/long) | Decimal |

### Import order (parents before children — Lookups need the target rows to exist)
1. **Masters/geo:** `State` → `District` → `UnitType` → `Unit` → `Rank` →
   `Designation` → `Employee` → `Court`
2. **Lookups:** `CaseCategory`, `GravityOffence`, `CaseStatusMaster`,
   `CasteMaster`, `ReligionMaster`, `OccupationMaster`
3. **Crime classification:** `CrimeHead` → `CrimeSubHead`; `Act` → `Section`;
   `CrimeHeadActSection`
4. **Core transactional:** `CaseMaster` → `ComplainantDetails`, `Victim`,
   `Accused`, `ActSectionAssociation`, `ArrestSurrender`, `ChargesheetDetails`,
   `Inv_OccuranceTime`, `inv_arrestsurrenderaccused`

## Fastest way to create the tables
Use Data Store's **CSV import** — importing a CSV auto-creates the table with
columns inferred from the header, then bulk-loads the rows. Seed CSVs for the
backbone tables are in [`catalyst/seed/`](./seed) (generated by
[`gen_seed.py`](./gen_seed.py)). Import them in the order above, then convert the
`*ID` FK columns to **Lookup** columns pointing at their parent table.

## Table → column reference (backbone subset that backs the current UI)

**State** — `StateID`(Num,PK) · `StateName`(Text) · `NationalityID`(Num) · `Active`(Bool)
**District** — `DistrictID`(Num,PK) · `DistrictName`(Text) · `StateID`(Lookup→State) · `Active`(Bool)

> ⚠️ **X2 (modeling shortcut, stated explicitly per `RESEARCH_AND_PLAN.md`
> §1.4c):** the 8 seeded `District` rows are revenue districts (Bengaluru
> Urban, Mysuru, Belagavi, Kalaburagi, Dakshina Kannada, Tumakuru, Ballari,
> Shivamogga). Karnataka's real jurisdictional map also has **commissionerates**
> (e.g. Bengaluru City) as a distinct unit type under their own CP/DCP
> structure, not a revenue district with police stations underneath it -
> Bengaluru City Police's DCP divisions are a different hierarchy shape than
> an SP's stations across a revenue district. This schema folds both into
> `District` for now. Anything reading `District`-level counts (`/districts`,
> `districtStats.ts`, `caseFacts.json`'s `districtId`) is reporting revenue-
> district totals, not a true commissionerate-vs-district split - read
> "Bengaluru Urban" as the seeded stand-in for that whole metro area, not a
> literal SP-run revenue district the way Shivamogga or Ballari are. Fixing
> this for real means adding a `UnitType` distinction the UI actually reads,
> not just renaming a row - tracked as real follow-up, not done here.
**UnitType** — `UnitTypeID`(Num,PK) · `UnitTypeName`(Text) · `CityDistState`(Text) · `Hierarchy`(Num) · `Active`(Bool)
**Unit** — `UnitID`(Num,PK) · `UnitName`(Text) · `TypeID`(Lookup→UnitType) · `ParentUnit`(Num) · `StateID`(Lookup→State) · `DistrictID`(Lookup→District) · `Active`(Bool)
**Rank** — `RankID`(Num,PK) · `RankName`(Text) · `Hierarchy`(Num) · `Active`(Bool)
**Designation** — `DesignationID`(Num,PK) · `DesignationName`(Text) · `SortOrder`(Num) · `Active`(Bool)
**Employee** — `EmployeeID`(Num,PK) · `DistrictID`(Lookup→District) · `UnitID`(Lookup→Unit) · `RankID`(Lookup→Rank) · `DesignationID`(Lookup→Designation) · `KGID`(Text) · `FirstName`(Text) · `EmployeeDOB`(Date) · `GenderID`(Num) · `AppointmentDate`(Date)
**Court** — `CourtID`(Num,PK) · `CourtName`(Text) · `DistrictID`(Lookup→District) · `StateID`(Lookup→State) · `Active`(Bool)
**CaseCategory** — `CaseCategoryID`(Num,PK) · `LookupValue`(Text)  *(FIR, UDR, Zero FIR, PAR)*
**GravityOffence** — `GravityOffenceID`(Num,PK) · `LookupValue`(Text)  *(Heinous, Non-Heinous)*
**CaseStatusMaster** — `CaseStatusID`(Num,PK) · `CaseStatusName`(Text)  *(Under Investigation, Charge Sheeted, Closed, Open)*
**CrimeHead** — `CrimeHeadID`(Num,PK) · `CrimeGroupName`(Text) · `Active`(Bool)
**CrimeSubHead** — `CrimeSubHeadID`(Num,PK) · `CrimeHeadID`(Lookup→CrimeHead) · `CrimeHeadName`(Text) · `SeqID`(Num)
**Act** — `ActCode`(Text,PK) · `ActDescription`(Text) · `ShortName`(Text) · `Active`(Bool)
**Section** — `ActCode`(Lookup→Act) · `SectionCode`(Text) · `SectionDescription`(Text) · `Active`(Bool)
**CaseMaster** — `CaseMasterID`(Num,PK) · `CrimeNo`(Text) · `CaseNo`(Text) · `CrimeRegisteredDate`(Date) · `PolicePersonID`(Lookup→Employee) · `PoliceStationID`(Lookup→Unit) · `CaseCategoryID`(Lookup→CaseCategory) · `GravityOffenceID`(Lookup→GravityOffence) · `CrimeMajorHeadID`(Lookup→CrimeHead) · `CrimeMinorHeadID`(Lookup→CrimeSubHead) · `CaseStatusID`(Lookup→CaseStatusMaster) · `CourtID`(Lookup→Court) · `IncidentFromDate`(DateTime) · `IncidentToDate`(DateTime) · `InfoReceivedPSDate`(DateTime) · `latitude`(Decimal) · `longitude`(Decimal) · `BriefFacts`(Text)
**ComplainantDetails** — `ComplainantID`(Num,PK) · `CaseMasterID`(Lookup→CaseMaster) · `ComplainantName`(Text) · `AgeYear`(Num) · `OccupationID`(Lookup→OccupationMaster) · `ReligionID`(Lookup→ReligionMaster) · `CasteID`(Lookup→CasteMaster) · `GenderID`(Num)
**Victim** — `VictimMasterID`(Num,PK) · `CaseMasterID`(Lookup→CaseMaster) · `VictimName`(Text) · `AgeYear`(Num) · `GenderID`(Num) · `VictimPolice`(Text)
**Accused** — `AccusedMasterID`(Num,PK) · `CaseMasterID`(Lookup→CaseMaster) · `AccusedName`(Text) · `AgeYear`(Num) · `GenderID`(Num) · `PersonID`(Text — **global person register handle**, `KA-Pnnnn`; see below)
**ActSectionAssociation** — `CaseMasterID`(Lookup→CaseMaster) · `ActID`(Lookup→Act) · `SectionID`(Lookup→Section) · `ActOrderID`(Num) · `SectionOrderID`(Num)
**ChargesheetDetails** — `CSID`(Num,PK) · `CaseMasterID`(Lookup→CaseMaster) · `csdate`(DateTime) · `cstype`(Text) · `PolicePersonID`(Lookup→Employee)

### `Accused.PersonID` — the person register

`PersonID` is the organizers' cross-case person key, and the column the whole
person-spine (`/persons/[personId]`, repeat-offender tracking, entity fusion)
hangs off. It holds a **global** handle — `KA-P0001`…`KA-P0047` in the current
seed — not a per-case or per-scenario label:

- **One ID per human, dataset-wide.** Two `Accused` rows sharing a `PersonID`
  are an assertion that they are the same person. In the current seed 47
  people occupy 53 rows; the 6 duplicated IDs are people charged in more than
  one FIR.
- **Stable.** An ID, once assigned, belongs to that person permanently.
  Re-seeding must not renumber existing people.
- **Names may differ across rows with the same ID** — "Suresh Naik" vs
  "Suresh N." — deliberately. `AccusedName` is what a given FIR recorded;
  `PersonID` is who it actually was.

Before P1.1 this column held scenario-local labels (`A1`…`A4`) that collided
across scenarios, making 17 different people share one ID. If you write a new
generator, do not reintroduce per-scenario numbering here — see
`catalyst/dataset-v2/README.md` for the token/index/glossary distinction and
`RESEARCH_AND_PLAN.md` §5.3(a).

## Extended tables

6 more tables the ER diagram defines beyond the 21-table backbone above.

**Built in the generator, not yet live** (P1.5, 2026-08-30 — see
`catalyst/dataset-v2/lookups.json` and `demographics.mjs`; live import is
P1.6, same as everything else in `catalyst/README.md`'s drift table):

**OccupationMaster** — `OccupationID`(Num,PK) · `OccupationName`(Text). 11
rows (10 categories + "Not specified"). Referenced by
`ComplainantDetails.OccupationID`, populated for all ~5,003 complainants.

**ReligionMaster** — `ReligionID`(Num,PK) · `ReligionName`(Text). 8 rows
(Census of India categories + "Not specified"). Referenced by
`ComplainantDetails.ReligionID`, populated via a Karnataka-representative
statistical draw for all complainants except institutional ones (a bank's
nodal officer, a forest-department officer) where a personal religion
doesn't apply.

**Still not built — genuinely undecided, not deferred by oversight:**

**CasteMaster** — `caste_master_id`(Num,PK) · `caste_master_name`(Text).
Referenced by `ComplainantDetails.CasteID` (currently a plain Number column
with no lookup target). The "aggregate-only, victim-side, with
denominators" framing agreed in PLAN.md P1.5 resolved how to *present*
caste data, not what taxonomy to use for it — official SC/ST/OBC/General
categories (the basis for Prevention of Atrocities Act enforcement, and what
NCRB's own reporting uses) versus omitting caste from the dataset entirely.
See PLAN.md's "Decisions still open" for the actual choice.

**Not required by the current UI, so deferred:**

**ArrestSurrender** — `ArrestSurrenderID`(Num,PK) · `CaseMasterID`(Lookup→CaseMaster)
· `ArrestSurrenderTypeID`(Num — arrest vs voluntary surrender, lookup value)
· `ArrestSurrenderDate`(Date) · `ArrestSurrenderStateId`(Lookup→State)
· `ArrestSurrenderDistrictId`(Lookup→District) · `PoliceStationID`(Lookup→Unit)
· `IOID`(Lookup→Employee — investigating officer) · `CourtID`(Lookup→Court)
· `AccusedMasterID`(Lookup→Accused) · `IsAccused`(Bool) ·
`IsComplainantAccused`(Bool)

**inv_arrestsurrenderaccused** — junction table, one `ArrestSurrender` event
↔ multiple `Accused` (many-to-many). Minimum: `ArrestSurrenderID`(Lookup→ArrestSurrender)
· `AccusedMasterID`(Lookup→Accused).

**CrimeHeadActSection** — `CrimeHeadID`(Lookup→CrimeHead) · `ActCode`(Lookup→Act)
· `SectionCode`(Text) — maps a major crime head to its applicable act-sections.

If/when any of these three are needed, build these exact columns rather
than re-deriving from `Police_FIR_ER_Diagram.pdf`.

## How the current UI maps onto these tables
| UI element | Source |
|---|---|
| Districts (Bengaluru Urban, Mysuru…) | `District` |
| Case types (Theft, Assault, Fraud, Burglary) | `CrimeSubHead` (under `CrimeHead`) |
| Case files (FIR-1001…) | `CaseMaster` (+ `Victim`/`Accused`/`ComplainantDetails`) |
| District clearance rate | `COUNT(CaseStatusID IN chargesheeted/closed) / COUNT(*)` per district |
| Year trend sparkline | `COUNT(*)` grouped by `YEAR(CrimeRegisteredDate)` + district |
| Sections invoked (379 IPC…) | `ActSectionAssociation` → `Section` |
| `/socio-economic` occupation/religion breakdown | `ComplainantDetails.OccupationID/ReligionID` → `OccupationMaster`/`ReligionMaster` |

*This table predates the P2/P4 rebuild and is otherwise stale (still
describes the old mock `FIR-1001…` case files) — the row above is accurate,
the rest needs a proper re-sweep, not attempted here.*

The investigation-board evidence/graph/AI data has **no** table in this FIR
schema — keep it in **Catalyst NoSQL** (document-shaped) or as mock.

## CrimeNo format (per the ER diagram)
`1 digit Case Category + 4 digit District + 4 digit Unit + 4 digit Year + 5 digit serial`
e.g. FIR `1 4401 0006 2026 00001` → `144010006202600001`. `CaseNo` = `YYYY` + 5-digit serial (the last 9 digits).
